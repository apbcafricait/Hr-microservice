import React from 'react'

const CandidateShortlist = () => {
//     CandidateShortlist.jsx

// A page to shortlist candidates for a particular role.
// HR can review applicants, score them based on qualifications, and move them to the next stage (e.g., interview).
  return (
    <div>CandidateShortlist</div>
  )
}

export default CandidateShortlist